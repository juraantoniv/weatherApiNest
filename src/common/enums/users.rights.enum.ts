export enum ERights {
  Costumer = 'costumer',
  Seller = 'seller',
  Manager = 'manager',
  Admin = 'admin',
}

export enum EType {
  Premium = 'premium',
  Default = 'default',
}

export enum EUserBanned{
  ACTIVE = 'active',
  BANNED = 'banned',
}